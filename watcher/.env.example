# Watcher-specific env vars (subset of root .env.example).
# Copy to watcher/.env or use the root .env — both are read by dotenv::dotenv().

SEPOLIA_WS_URL=wss://eth-sepolia.g.alchemy.com/v2/YOUR_KEY
ORDER_BOOK_ADDRESS=
SETTLEMENT_ADDRESS=
WATCHER_FROM_BLOCK=0
