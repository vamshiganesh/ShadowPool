# Prover service — copy to .env and fill in before on-chain submission
SEPOLIA_RPC_URL=
PROVER_PRIVATE_KEY=

# Set to 1 to broadcast settle() after proving (requires deployed contracts + funded key)
SUBMIT_ON_CHAIN=0
