import { PageShell } from '@/components/ui/PageShell'

export function OrdersPage() {
  return (
    <PageShell
      title="My Orders"
      description="View commitment history, match status, and settlement state."
      eyebrow="Orders"
      layout="app"
    />
  )
}
