import { PageShell } from '@/components/ui/PageShell'

export function TradePage() {
  return (
    <PageShell
      title="Trading Terminal"
      description="Submit private order commitments and monitor matching lifecycle."
      eyebrow="App"
      layout="app"
    />
  )
}
