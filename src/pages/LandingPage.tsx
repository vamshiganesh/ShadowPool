import { PageShell } from '@/components/ui/PageShell'

export function LandingPage() {
  return (
    <PageShell
      title="ShadowPool Protocol"
      description="ZK dark pool trading for Ethereum. Private commitments, off-chain matching, Groth16 proofs, atomic settlement."
      eyebrow="Landing"
      layout="marketing"
    />
  )
}
